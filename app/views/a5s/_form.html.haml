= form_for @a5 do |f|
  - if @a5.errors.any?
    #error_explanation
      %h2= "#{pluralize(@a5.errors.count, "error")} prohibited this a5 from being saved:"
      %ul
        - @a5.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
