%h1 Listing a2s

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @a2s.each do |a2|
      %tr
        %td= link_to 'Show', a2
        %td= link_to 'Edit', edit_a2_path(a2)
        %td= link_to 'Destroy', a2, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New A2', new_a2_path
